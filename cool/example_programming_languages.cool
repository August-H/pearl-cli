CREATE TABLE users (
    user_id : INT PRIMARY KEY,
    name : STRING NOT NULL,
    email : STRING UNIQUE NOT NULL,
    address : STRING
);

CREATE TABLE products (
    product_id : INT PRIMARY KEY,
    name : STRING NOT NULL,
    description : STRING,
    price : FLOAT NOT NULL,
    stock : INT NOT NULL DEFAULT 0
);

CREATE TABLE orders (
    order_id : INT PRIMARY KEY,
    user_id : INT REFERENCES users(user_id),
    order_date : STRING,
    total_amount : FLOAT NOT NULL
);

INSERT INTO users (user_id, name, email, address) 
VALUES (1, "John Doe", "john.doe@example.com", "123 Amazon St, Seattle");

INSERT INTO products (product_id, name, description, price, stock) 
VALUES (101, "Kindle Paperwhite", "E-reader with 6.8\" display", 139.99, 50);

INSERT INTO products (product_id, name, description, price, stock) 
VALUES (102, "Echo Dot", "Smart speaker with Alexa", 49.99, 100);

INSERT INTO orders (order_id, user_id, order_date, total_amount) 
VALUES (5001, 1, "2023-10-27", 189.98);
